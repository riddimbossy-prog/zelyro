# Security

OWASP: HTTPS, session cookies / JWT, refresh rotation, RLS, rate limits, input sanitization, signed URLs, webhook verification, audit logs, RBAC.

Never trust: user id, price, commission, purchase status from the client.

Secrets: GitHub Secrets, DigitalOcean, server env. Anon/public keys only in Flutter.
