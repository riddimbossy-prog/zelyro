# VerzZify Live

Scheduled events, free or PPV. Access = `live_entitlements` row. Join uses a short-lived stream token from the API (Agora / Daily adapter). Comments/reactions optional. Unauthorized clients never receive the ingest URL.
