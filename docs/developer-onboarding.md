# Developer onboarding

1. Read `docs/architecture.md`.  
2. You receive delegated cloud access, never master logins.  
3. Copy env from the secrets manager — never from Slack.  
4. Run the web slice locally; schema applies from `migrations/`.  
5. Do not add a payment provider by pasting keys into Flutter.
