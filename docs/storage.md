# Storage

S3 masters are private. The API issues presigned uploads after MIME/size checks. FFmpeg workers emit stream renders + waveforms. Signed downloads require an entitlement (free_download, purchase, or admin).
